import React from "react";
import ReactDOM from "react-dom/client";
import ModLoaderUI from "./src/App"; // Ensure this path is correct

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ModLoaderUI />
  </React.StrictMode>
);
