import React from "react";
import ReactDOM from "react-dom/client";
import ModLoaderUI from "./App"; // Ensure "App.jsx" is in the same directory

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ModLoaderUI />
  </React.StrictMode>
);
